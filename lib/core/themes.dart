import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Colors.orange; // Birincil renk
  static const Color secondary = Colors.white; // İkincil renk (ör: arka plan)
  static const Color accent = Colors.grey; // Yardımcı renk (ör: kenarlıklar)
  static const Color textPrimary = Colors.black; // Ana metin rengi
  static const Color textSecondary = Colors.orange; // Yardımcı metin rengi
}